export const writeNoteOnOverallReport = (text: string) => {
  // const dirPath = path.join(__dirname, "../reports");
  // const fullPath = path.join(dirPath, "report.txt");
  // if (!fs.existsSync(dirPath)) fs.mkdirSync(dirPath);
  // const newText = fs.readFileSync(fullPath, "utf-8") + "\n" + text;
  // fs.writeFileSync(fullPath, newText);
};
